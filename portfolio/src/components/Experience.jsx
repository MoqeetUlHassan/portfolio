import React from 'react';
import styled from 'styled-components';

const Section = styled.div`
  height: 100vh;
  scroll-snap-align: center;
`;

const Experience = () => {
  return <Section>EXP</Section>;
};

export default Experience;
